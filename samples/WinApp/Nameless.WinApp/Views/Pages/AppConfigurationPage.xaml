<Page x:Class="Nameless.WinApp.Views.Pages.AppConfigurationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:loc="clr-namespace:Nameless.WinApp"

      x:Name="AppConfigurationPageRoot"

      mc:Ignorable="d" 

      Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <StackPanel Margin="24">
        <ui:TextBlock FontTypography="Title"
                      Margin="0 0 0 20"
                      Text="{Binding [AppConfigurationPage_Title], Source={x:Static loc:L10N.Instance}}" />
        <ui:CardControl Icon="{ui:ImageIcon '/Resources/application_64x64.png'}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_VersionCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1"
                                  Text="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.AppVersion, Mode=OneTime}" />
                </Grid>
            </ui:CardControl.Header>
            <ui:Button Appearance="Primary"
                       Command="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.PerformSystemUpdateCommand, Mode=OneTime}"
                       Content="{Binding [AppConfigurationPage_VersionCard_Button_Caption], Source={x:Static loc:L10N.Instance}}"
                       Cursor="Hand"
                       Icon="{ui:SymbolIcon Globe32}"
                       Padding="30 8" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon DatabaseMultiple20}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_ApplicationBackupCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1" Text="{Binding [AppConfigurationPage_ApplicationBackupCard_Message], Source={x:Static loc:L10N.Instance}}" />
                </Grid>
            </ui:CardControl.Header>
            <ui:Button Appearance="Success"
                       Cursor="Hand"
                       Command="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.PerformApplicationBackupCommand, Mode=OneTime}"
                       Content="{Binding [AppConfigurationPage_ApplicationBackupCard_Button_Caption], Source={x:Static loc:L10N.Instance}}"
                       Icon="{ui:SymbolIcon DatabaseArrowDown20}"
                       Padding="30 8" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon DatabaseLightning20}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_ApplicationRestoreCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1" Text="{Binding [AppConfigurationPage_ApplicationRestoreCard_Message], Source={x:Static loc:L10N.Instance}}" />
                </Grid>
            </ui:CardControl.Header>
            <ui:Button Appearance="Success"
                       Cursor="Hand"
                       Command="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.PerformApplicationRestoreCommand, Mode=OneTime}"
                       Content="{Binding [AppConfigurationPage_ApplicationRestoreCard_Button_Caption], Source={x:Static loc:L10N.Instance}}"
                       Icon="{ui:SymbolIcon DatabaseArrowUp20}"
                       Padding="30 8" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon Color24}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_ThemeCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1"
                                  Text="{Binding [AppConfigurationPage_ThemeCard_Message], Source={x:Static loc:L10N.Instance}}" />
                </Grid>
            </ui:CardControl.Header>
            <ComboBox Cursor="Hand"
                      ItemsSource="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.AvailableThemes, Mode=OneTime}"
                      SelectedValue="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.CurrentTheme, Mode=TwoWay}"
                      IsSynchronizedWithCurrentItem="True"
                      MinWidth="200" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon ArrowExit20}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_ExitBehaviorCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1"
                                  Text="{Binding [AppConfigurationPage_ExitBehaviorCard_Message], Source={x:Static loc:L10N.Instance}}" />
                </Grid>
            </ui:CardControl.Header>
            <CheckBox Cursor="Hand"
                      IsChecked="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.CurrentConfirmBeforeExit, Mode=TwoWay}" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon FolderGlobe20}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_AppDirectoryPathCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1"
                                  Text="{Binding [AppConfigurationPage_AppDirectoryPathCard_Message], Source={x:Static loc:L10N.Instance}}" />
                </Grid>
            </ui:CardControl.Header>
            <ui:Button Appearance="Primary"
                       Cursor="Hand"
                       Command="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.OpenApplicationDataDirectoryCommand, Mode=OneTime}"
                       Content="{Binding [AppConfigurationPage_AppDirectoryPathCard_Button_Caption], Source={x:Static loc:L10N.Instance}}"
                       Icon="{ui:SymbolIcon Open48}"
                       Padding="30 8" />
        </ui:CardControl>

        <ui:CardControl Icon="{ui:SymbolIcon CodeTextEdit20}"
                        Margin="0 0 0 10">
            <ui:CardControl.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ui:TextBlock FontTypography="BodyStrong"
                                  Grid.Row="0"
                                  Text="{Binding [AppConfigurationPage_ApplicationLogCard_Title], Source={x:Static loc:L10N.Instance}}" />
                    <ui:TextBlock Grid.Row="1"
                                  Text="{Binding [AppConfigurationPage_ApplicationLogCard_Message], Source={x:Static loc:L10N.Instance}}" />
                </Grid>
            </ui:CardControl.Header>
            <ui:Button Appearance="Primary"
                       Cursor="Hand"
                       Command="{Binding ElementName=AppConfigurationPageRoot, Path=ViewModel.OpenApplicationLogFileCommand, Mode=OneTime}"
                       Content="{Binding [AppConfigurationPage_ApplicationLogCard_Button_Caption], Source={x:Static loc:L10N.Instance}}"
                       Icon="{ui:SymbolIcon Open48}"
                       Padding="30 8" />
        </ui:CardControl>

    </StackPanel>
</Page>
