namespace Nameless.FileStorage.FileSystem.Properties {
    internal static class Resources {
        #region Internal Static Read-Only Fields

        internal static readonly string TheFileDoesNotExistsMessage = "The file does not exists.";

        internal static readonly string TheDirectoryDoesNotExistsMessage = "The directory does not exists.";

        #endregion
    }
}