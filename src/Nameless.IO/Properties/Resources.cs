namespace Nameless.IO.Properties {
    // Tries to emulate the Resources.resx from the .NET Framework.
    internal static class Resources {
        #region Internal Static Properties

        internal static string MaximunFileLengthShouldBeGreaterThanZero { get; } = "Maximun file length should be greater than zero.";
        internal static string MaximunFileCountShouldBeGreaterThanZero { get; } = "Maximun file count should be greater than zero.";
        internal static string BufferSizeExceedsMaximunFileLength { get; } = "Buffer size exceeds maximun file length.";

        #endregion
    }
}