namespace Nameless.IO;

/// <summary>
/// Defines properties to represent a directory.
/// </summary>
public interface IDirectory {
    /// <summary>
    /// Gets the path to the directory.
    /// </summary>
    string Path { get; }
}