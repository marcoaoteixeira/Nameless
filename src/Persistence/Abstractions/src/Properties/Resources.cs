namespace Nameless.Persistence.Properties {
    // Tries to emulate the Resources.resx from the .NET Framework.
    internal static class Resources {
        #region Internal Static Properties

        internal static string ID_Member_Not_Found_MessageFormatter { get; } = "Could not found ID member in type '{0}'. If you're using convention named property or field, remember that the valid names are: '{0}'.";

        #endregion
    }
}